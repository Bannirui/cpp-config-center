#include <gtest/gtest.h>

// MySQL integration tests - run against a MySQL container
// These tests are skipped when no MySQL instance is available

TEST(MySqlStorageTest, DISABLED_Placeholder) {
    // MySQL tests require a running MySQL instance
    SUCCEED();
}
